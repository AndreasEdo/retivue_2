# PyTorch CPU wheel:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
python-multipart

torch
timm

albumentations
opencv-python-headless
numpy
pillow
scipy

grad-cam

# --- DB / auth / storage (v2 telemedicine) ---
motor
pymongo
python-jose[cryptography]
bcrypt
cloudinary
python-dotenv
pydantic-settings
email-validator
